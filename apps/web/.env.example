# Browser-side calls (inlined at build time by Next.js).
NEXT_PUBLIC_API_URL=http://localhost:4000

# Server-side calls from Next.js to the API. Inside Docker this becomes http://api:4000.
API_URL=http://localhost:4000
