/**
 * Everything that writes. A screen declares its fields, points at its server
 * action, and these components do the rest.
 */
export { ActionButton, type ActionButtonProps } from "./action-button";
export { RecordDialog, type RecordDialogProps } from "./record-dialog";
export { RecordForm, type RecordFormProps } from "./record-form";
export { RowActions, type RowActionsProps } from "./row-actions";
